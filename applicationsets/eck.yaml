apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: eck
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - merge:
        mergeKeys:
          - server
        generators:
          - clusters:
              selector:
                matchExpressions:
                  - key: enable_eck
                    operator: In
                    values: ['true']
              values:
                chartOperator: eck-operator
                chartECK: eck-stack
                chartRepository: https://helm.elastic.co
          - clusters:
              selector:
                matchLabels:
                  env: lab
              values:
                operatorChartVersion: 3.4.0
                stackChartVersion: 0.19.0
  template:
    metadata:
      name: "eck-{{.values.operatorChartVersion}}-{{.name}}"
    spec:
      project: default
      sources:
        - repoURL: '{{ .values.chartRepository}}'
          targetRevision: '{{ .values.operatorChartVersion}}'
          chart: '{{ .values.chartOperator}}'
          helm:
            releaseName: eck-operator
            valueFiles:
              - $repo/values/eck/{{ .name }}/operator-values.yaml
        - repoURL: '{{ .values.chartRepository}}'
          targetRevision: '{{ .values.stackChartVersion}}'
          chart: '{{ .values.chartECK}}'
          helm:
            releaseName: eck-stack
            valueFiles:
              - $repo/values/eck/{{ .name }}/stack-values.yaml
        - repoURL: https://github.com/fjvi13/homelab-kubernetes-gitops.git
          targetRevision: HEAD
          ref: repo
      destination:
        name: '{{ .name }}'
        namespace: elastic-system
      syncPolicy:
        syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
        - SkipDryRunOnMissingResource=true
