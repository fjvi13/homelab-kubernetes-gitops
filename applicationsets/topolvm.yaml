apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: topolvm
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - merge:
        mergeKeys:
          - server
        generators:
          - clusters:
              selector:
                matchExpressions:
                  - key: enable_topolvm
                    operator: In
                    values: ['true']
              values:
                chart: topolvm
                chartRepository: https://topolvm.github.io/topolvm/
          - clusters:
              selector:
                matchLabels:
                  env: lab
              values:
                chartVersion: 16.0.1
  template:
    metadata:
      name: "topolvm-{{.name}}"
      annotations:
        argocd.argoproj.io/manifest-generate-paths: "values/topolvm/{{ .name }}"
    spec:
      project: addons
      sources:
        - repoURL: '{{ .values.chartRepository}}'
          targetRevision: '{{ .values.chartVersion}}'
          chart: '{{ .values.chart}}'
          helm:
            releaseName: topolvm
            valueFiles:
              - $repo/values/topolvm/{{ .name }}/values.yaml
        - repoURL: https://github.com/fjvi13/homelab-kubernetes-gitops.git
          targetRevision: HEAD
          ref: repo
      destination:
        name: '{{ .name }}'
        namespace: topolvm-system
      syncPolicy:
        syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true