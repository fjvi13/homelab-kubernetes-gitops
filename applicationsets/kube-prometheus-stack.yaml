apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: kube-prometheus-stack
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - merge:
        mergeKeys:
          - server
        generators:
          - clusters:
              selector:
                matchExpressions:
                  - key: enable_prometheus
                    operator: In
                    values: ['true']
              values:
                chart: kube-prometheus-stack
                chartRepository: https://prometheus-community.github.io/helm-charts
          - clusters:
              selector:
                matchLabels:
                  env: lab
                  # cluster: central-cluster
              values:
                chartVersion: 78.5.0
                branchReference: HEAD
  template:
    metadata:
      name: "prometheus-{{.name}}"
    spec:
      project: addons
      sources:
        - repoURL: '{{ .values.chartRepository}}'
          targetRevision: '{{ .values.chartVersion}}'
          chart: '{{ .values.chart}}'
          helm:
            releaseName: prometheus
            valueFiles:
              - $repo/values/kube-prometheus-stack/{{ .name }}/values.yaml
        - repoURL: https://github.com/fjvi13/homelab-kubernetes-gitops.git
          targetRevision: '{{ .values.branchReference}}'
          ref: repo
      destination:
        name: '{{ .name }}'
        namespace: monitoring
      syncPolicy:
        syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true